const State = {
	/* =================================================
	   DEFAULT
	================================================= */

	defaultForm() {
		return {
			id: null,

			name: '',

			phone: '',

			email: '',

			address: '',

			description: '',
		};
	},

	/* =================================================
	   FORM
	================================================= */

	form: {},

	/* =================================================
	   STATUS
	================================================= */

	loading: false,

	submitting: false,

	/* =================================================
	   SET CUSTOMER
	================================================= */

	setCustomer(customer = {}) {
		this.form = {
			...this.form,

			...customer,
		};
	},

	/* =================================================
	   SET FIELD
	================================================= */

	setField(key, value) {
		if (key in this.form) {
			this.form[key] = value;
		}
	},

	/* =================================================
	   RESET
	================================================= */

	reset() {
		this.form = this.defaultForm();
	},
};

State.reset();

export default State;
